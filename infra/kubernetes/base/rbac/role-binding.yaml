apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: pernforge-secret-reader-binding

subjects:
  - kind: ServiceAccount
    name: pernforge-server-sa

roleRef:
  kind: Role
  name: pernforge-secret-reader
  apiGroup: rbac.authorization.k8s.io